#
# plot of the potential
#
set title "Potential"			# set title
set xlabel "radius [m]"			# set x axis lable
set ylabel "Potential [V]"		# set y axis lable
set term wxt 1					# set figure ID, prevention of overwriting of existing figures
$data << EOD
@
EOD
plot '$data' using 1:2 with lines notitle 	