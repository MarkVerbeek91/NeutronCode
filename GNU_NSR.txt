#
# plot of Neutron source rate
#
set title "Neutron source rate"			# set title
set xlabel "radius [m]"			# set x axis lable
set ylabel "foo"		# set y axis lable
set term wxt 8					# set figure ID, prevention of overwriting of existing figures
$data << EOD
@
EOD
plot '$data' using 1:2 with lines notitle 	